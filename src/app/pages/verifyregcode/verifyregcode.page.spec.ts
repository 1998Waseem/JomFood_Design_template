import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VerifyregcodePage } from './verifyregcode.page';

describe('VerifyregcodePage', () => {
  let component: VerifyregcodePage;
  let fixture: ComponentFixture<VerifyregcodePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VerifyregcodePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
